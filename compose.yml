version: "3"

services:
  clickhouse:
    image: clickhouse/clickhouse-server
    container_name: clickhouse
    ports:
      - "8123:8123"
      - "9000:9000"
    networks:
      - nixbuilder


networks:
  nixbuilder:
    driver: bridge